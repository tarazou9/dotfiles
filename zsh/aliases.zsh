alias reload!='. ~/.zshrc'

alias cls='clear' # Good 'ol Clear Screen command

alias printcolors='for code ({000..255}) print -P -- "$code: %F{$code}This is how your text would look like%f"'

alias sonar-scanner='sonar-scanner -Dsonar.host.url=$SONAR_URL'
